import { Map } from "../components/Map";
import styles from "./Home.module.css";

const Home = () => {
   return (
      //<div className={styles.container}>
      <div>
         <Map />
      </div>
   );
};

export default Home;
